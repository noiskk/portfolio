services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC (Spring AI 사용)
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
